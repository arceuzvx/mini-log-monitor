services:
  log-monitor:
    build: .
    environment:
      - SLACK_WEBHOOK=${SLACK_WEBHOOK}
      - LOG_FILE=/logs/app.log
      - COOLDOWN_SECONDS=30
    volumes:
      - ./logs:/logs
    restart: unless-stopped